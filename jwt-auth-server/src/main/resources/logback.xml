<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">
    <include resource="org/springframework/boot/logging/logback/base.xml"/>

    <!-- Kod podsystemu z enumeracji KodPodsystemu. Przykład recepta-ws -->
    <property name="APP_CODE" value="e-recepta-ikp"/>

    <appender name="ROLLING-FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS}:${HOSTNAME}::%X{UUID} %-5level %X{PID} ---  [%thread] %logger{36} : %msg%n</pattern>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>/var/app-log/${APP_CODE}_%d{yyyyMMdd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>100MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>2</maxHistory>
        </rollingPolicy>
    </appender>

    <!--<appender name="STDOUT"-->
              <!--class="ch.qos.logback.core.ConsoleAppender">-->
        <!--<encoder>-->
            <!--<pattern>-->
                <!--<pattern>%d{yyyy-MM-dd HH:mm:ss.SSS}:${HOSTNAME}::%X{UUID} %-5level %X{PID} -&#45;&#45;  [%thread] %logger{36} : %msg%n</pattern>-->
            <!--</pattern>-->
        <!--</encoder>-->
    <!--</appender>-->

    <logger name="org.springframework" level="INFO"/>
    <logger name="org.apache.cxf" level="INFO"/>
    <logger name="pl.gov.csioz.p1" level="INFO"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ROLLING-FILE"/>
        <!--<appender-ref ref="STDOUT" />-->
    </root>

</configuration>
