<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:context="http://www.springframework.org/schema/context"
             xsi:schemaLocation="http://www.springframework.org/schema/mvc 
		http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans 
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context 
		http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->

    <!-- Enables the Spring MVC @Controller programming model -->
    <annotation-driven>
        <message-converters>
            <beans:bean class="org.springframework.http.converter.StringHttpMessageConverter"/>
            <beans:bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter"/>
        </message-converters>
    </annotation-driven>

    <!-- static resources like Javascript, images, css and its location -->
    <!-- ExtJS -->
    <resources mapping="/resources/**" location="/resources/" />
    <resources mapping="/ext/**" location="/ext/"/>
    <resources mapping="/app/**" location="/app/"/>
    <resources mapping="/classic/**" location="/classic/"/>
    <resources mapping="/modern/**" location="/modern/"/>
    <resources mapping="/archive/**" location="/archive/"/>
    <resources mapping="/index.html" location="/index.html"/>
    <resources mapping="/cache.appcache" location="/cache.appcache"/>
    <resources mapping="/cache.appcache" location="/cache.appcache"/>
    <resources mapping="/classic.json" location="/classic.json"/>
    <resources mapping="/modern.json" location="/modern.json"/>

    <!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
    <beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <beans:property name="prefix" value="/WEB-INF/views/" />
        <beans:property name="suffix" value=".jsp" />
    </beans:bean>

    <!-- Factory bean that creates the MongoDB instance -->
    <beans:bean id="mongo" class="org.springframework.data.mongodb.core.MongoFactoryBean">
        <beans:property name="host" value="localhost"/>
    </beans:bean>
	
    <!-- Define the MongoTemplate which handles connectivity with MongoDB -->
    <beans:bean id="mongoTemplate" class="org.springframework.data.mongodb.core.MongoTemplate">
        <beans:constructor-arg name="mongo" ref="mongo" />
        <beans:constructor-arg name="databaseName" value="patients" />
    </beans:bean>
	
    <!-- Use this post processor to translate any MongoExceptions thrown in @Repository annotated classes -->
    <beans:bean class="org.springframework.dao.annotation.PersistenceExceptionTranslationPostProcessor"/>
   
    <context:component-scan base-package="com.grinno.patients" />
	
</beans:beans>
